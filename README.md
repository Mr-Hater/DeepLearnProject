本项目完完全全是一个半成品，目前因为时间原因，只做了简单的论文显示和相关模型训练。
但训练过程不会在网页中显示（做过几个想法但是都不是很好就没上传），所以使用该项目训练模型，训练过程的显示依旧是在ide中进行显示。
用这个来操作模型训练可能就是比起原来的LibEER那种命令行输入会简单一些，毕竟变成了一种可视化的东西，并且不是输入一串命令而是选择好，按个按钮就可以运行。
除此之外就是论文的显示，还有想着做一个markdown文档显示和编辑的功能，因为bug也就不能使用还未上传。
项目使用的技术是layui+django。其中因为我方向是脑电接口，目前里面有一个脑电的模型可以使用，不过数据集之类的东西也是要下载的，只能说这个项目比起一个完备的项目，不如说，算是一个用来模型训练的模板网站设计
